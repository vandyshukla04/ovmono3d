{"_comment": "WildBox Phase 1 category meta — loaded by tools/train_net.py eval-only path from the same directory as the config file.", "thing_classes": ["giraffe"], "thing_dataset_id_to_contiguous_id": {"1000": 0}}
